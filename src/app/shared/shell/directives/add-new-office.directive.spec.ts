import { AddNewOfficeDirective } from './add-new-office.directive';

describe('AddNewOfficeDirective', () => {
  it('should create an instance', () => {
    const directive = new AddNewOfficeDirective();
    expect(directive).toBeTruthy();
  });
});
